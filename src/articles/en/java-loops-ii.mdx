---
title: Java Loops II
description: 'How to solve the Hackerrank challenge in java, Java Loops II. We use the integers **a**, **b**, and **n** to create the following series: **(a + 2<sup>0</sup> . b), (a + 2 <sup>0</sup> . b + 2<sup>1</sup> . b), ..., (a + 2<sup>0</sup> . b + 2<sup>1</ sup> . b + ... + 2<sup>n-1</sup> . b)**. You are given **q** queries in the form of **a**, **b**, and **n**. For each query, print the series corresponding to the given **a**, **b**, and **n** values as a single line of space-separated **n** integers.'
createdAt: 2023-12-25 18:40
author: Douglas Matos
image: /images/thumb-hackerrank-java-loops-ii.png
lastModified: 2023-12-25 10:20
lang: en
tags: chanllenges, hackerrank, java
---
![Hackerrank - Java Loops II](/images/thumb-hackerrank-java-loops-ii.png)


### First let's read the challenge statement

We use the integers **a**, **b**, and **n** to create the following series: **(a + 2<sup>0</sup> . b), (a + 2<sup>0</sup> . b + 2<sup>1</sup> . b), ..., (a + 2<sup>0</sup> . b + 2<sup>1</sup> . b + ... + 2<sup>n-1</sup> . b)**

You are given **q** queries in the form of **a**, **b**, and **n**. For each query, print the series corresponding to the given **a**, **b**, and **n** vvalues as a single line of **n** space-separated integers.

### Input Format

The first line contains an integer, **q**, denoting the number of queries.
Each line **i** of the **q** subsequent lines contains three space-separated integers describing the respective ***a<sub>i</sub>, b<sub>i</sub> and n<sub>i</sub>***, values for that query.

### Constraints
- **{'0 <= q <= 500'}**
- **{'0 <= a, b <= 50'}**
- **{'0 <= n <= 15'}**

### Output Format

For each query, print the corresponding series on a new line. Each series must be printed in order as a single line of **N** space-separated integers.

### Sample Input

```bash
2
0 2 10
5 3 5
```

### Sample Output

```bash
2 6 14 30 62 126 254 510 1022 2046
8 14 26 50 98
```

### Explanation

We have two queries:

1. We use **{'a = 0, b = 2'}**, e **{'n = 10'}**, to produce some series **s<sub>1</sub>,...,s<sub>n-1</sub>**:
- **s<sub>0</sub> = 0 + 1 . 2 = 2**
- **s<sub>1</sub> = 0 + 1 . 2 + 2 . 2 = 6**
- **s<sub>2</sub> = 0 + 1 . 2 + 2 . 2 + 4 . 2 = 14**

...and so on.

Once we hit **n = 10**, we print the first ten terms as a single line of space-separated integers.

2. We use **{'a = 5, b = 3'}**, e **{'n = 5'}** to produce some series **s<sub>1</sub>,...,s<sub>n-1</sub>**:
- **s<sub>0</sub> = 5 + 1 . 3 = 8**
- **s<sub>0</sub> = 5 + 1 . 3 + 2 . 3 = 14**
- **s<sub>0</sub> = 5 + 1 . 3 + 2 . 3 + 4 . 3 = 26**
- **s<sub>0</sub> = 5 + 1 . 3 + 2 . 3 + 4 . 3 + 8 . 3 = 50**
- **s<sub>0</sub> = 5 + 1 . 3 + 2 . 3 + 4 . 3 + 8 . 3 + 16 . 3 = 98**

We then print each element of our series as a single line of space-separated values.

In the challenge, an initial code is left, which appears shortly after.

```java
import java.util.*;
import java.io.*;

class Solution{
    public static void main(String []argh){
        Scanner in = new Scanner(System.in);
        int t=in.nextInt();
        for(int i=0;i<t;i++){
            int a = in.nextInt();
            int b = in.nextInt();
            int n = in.nextInt();
        }
        in.close();
    }
}
```

Let's look at the solution, first create the variable **result** within the existing loop.
```java 
import java.util.*;
import java.io.*;

class Solution{
    public static void main(String []argh){
        Scanner in = new Scanner(System.in);
        int t=in.nextInt();
        for(int i=0;i<t;i++){
            int a = in.nextInt();
            int b = in.nextInt();
            int n = in.nextInt();
        
            double result = a;
        }


        in.close();
    }
}
```

Now create a "for" loop inside the existing loop with **j < n** as its termination condition
```java 
import java.util.*;
import java.io.*;

class Solution{
    public static void main(String []argh){
        Scanner in = new Scanner(System.in);
        int t=in.nextInt();
        for(int i=0;i<t;i++){
            int a = in.nextInt();
            int b = in.nextInt();
            int n = in.nextInt();

            double result = a;

            for (int j = 0; j < n; j++) {
            }
        }        

        in.close();
    }
}
```

Now, let's calculate the exponents.
```java 
import java.util.*;
import java.io.*;

class Solution{
    public static void main(String []argh){
        Scanner in = new Scanner(System.in);
        int t=in.nextInt();
        for(int i=0;i<t;i++){
            int a = in.nextInt();
            int b = in.nextInt();
            int n = in.nextInt();

            double result = a;

            for (int j = 0; j < n; j++) {
                double z = Math.pow(2, j);
            }
        }        

        in.close();
    }
}
```

Then assign the variable **result** to the multiplication of **2 * b**
```java 
import java.util.*;
import java.io.*;

class Solution{
    public static void main(String []argh){
        Scanner in = new Scanner(System.in);
        int t=in.nextInt();
        for(int i=0;i<t;i++){
            int a = in.nextInt();
            int b = in.nextInt();
            int n = in.nextInt();

            double result = a;

            for (int j = 0; j < n; j++) {
                double z = Math.pow(2, j);
                result += z * b;
            }
        }        

        in.close();
    }
}
```

Now just print the formatted values
```java 
import java.util.Scanner;

public class Solution {
    public static void main(String[] args) {

        Scanner in = new Scanner(System.in);
        int t = in.nextInt();

        for (int i = 0; i < t; i++) {
            int a = in.nextInt();
            int b = in.nextInt();
            int n = in.nextInt();

            double result = a;

            for (int j = 0; j < n; j++) { // "n" times
                double z = Math.pow(2, j);
                result += z * b;
                System.out.printf("%.0f ", result);
            }
            System.out.println();

        }

        in.close();
    }
}
```
